// import { Avatar } from 'antd';

// export const imageSource = ({ user }) => {
//   return user.image && user.image.url ? (
//     <Avatar size={50} src={user.image.url} />
//   ) : (
//     <Avatar size={50}>
//       {user?.name?.charAt(0).toUpperCase()}
//     </Avatar>
//   );
// };